provider "aws" {
  region = "us-east-1" 
  profile = "kalyancisco"

  # Add your AWS access and secret keys or use a different authentication method
  access_key = "#######"
  secret_key = "##################"
}
