provider "aws" {
  region = "us-east-1"  # Update with your desired region

  # Add your AWS access and secret keys or use a different authentication method
  access_key = "#######"
  secret_key = "#######"
}
